[pytest]
; exclude unrelated folders and all old tests
norecursedirs =
    .*
    .git
; more verbose summary (include skip/fail/error/warning), coverage
addopts = -rsfEw --cov=omp_thread_count --no-cov-on-fail
